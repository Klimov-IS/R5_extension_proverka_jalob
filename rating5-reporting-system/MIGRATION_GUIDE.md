# 📦 МИГРАЦИЯ ДАННЫХ КЛИЕНТОВ

Теперь когда структура таблицы готова, нужно перенести данные клиентов из старой таблицы.

---

## 🎯 ЦЕЛЬ

Автоматически мигрировать:
- ✅ Данные клиентов (название, статус, ссылки на папки)
- ✅ Артикулы по каждому клиенту
- ✅ Извлечь Folder IDs из Drive URLs
- ✅ Создать ClientID для каждого клиента

---

## 📝 ИНСТРУКЦИЯ

### ШАГ 1: Узнайте ID старой таблицы

1. Откройте вашу старую таблицу **"Отчет автоматизация"** с данными клиентов

2. Скопируйте **ID из URL** (часть между `/d/` и `/edit`):
   ```
   https://docs.google.com/spreadsheets/d/1ABC...XYZ/edit
                                              ^^^^^^^^
                                           Это ID таблицы
   ```

3. Сохраните ID - он понадобится на следующем шаге

---

### ШАГ 2: Добавьте скрипт миграции

1. Откройте вашу **НОВУЮ таблицу** Rating5_OPS:
   ```
   https://docs.google.com/spreadsheets/d/1D5xpmpYbhEYYKFg8CLzQ-bvyAJaK7pZAhtUdAuJjqhQ/edit
   ```

2. Откройте Apps Script:
   ```
   Extensions → Apps Script
   ```

3. Создайте новый файл:
   - Нажмите **"+"** рядом с Files
   - Выберите **"Script"**
   - Назовите файл: **`MigrateData`**

4. Скопируйте код из файла:
   - Откройте [`apps-script/MigrateData.gs`](apps-script/MigrateData.gs)
   - Скопируйте **весь код** (Ctrl+A, Ctrl+C)
   - Вставьте в новый файл MigrateData (Ctrl+V)

5. **ВАЖНО:** В начале файла найдите строку:
   ```javascript
   const OLD_SPREADSHEET_ID = ''; // ← ВСТАВЬТЕ ID СЮДА!
   ```

6. Вставьте ID старой таблицы между кавычками:
   ```javascript
   const OLD_SPREADSHEET_ID = '1ABC...XYZ';
   ```

7. Нажмите **"Сохранить"** (Ctrl+S)

---

### ШАГ 3: Проверьте подключение (опционально)

Перед миграцией можно проверить, что скрипт видит старую таблицу:

1. В выпадающем списке выберите функцию:
   ```
   testOldTableConnection
   ```

2. Нажмите **"Запустить"** (▶️)

3. Вы должны увидеть сообщение:
   ```
   ✅ Таблица найдена
   ✅ Лист "Клиенты" найден
   ✅ Строк данных: XX

   Можно запускать миграцию!
   ```

Если видите ошибку - проверьте, что:
- ID таблицы указан правильно
- У вас есть доступ к старой таблице
- В старой таблице есть лист "Клиенты"

---

### ШАГ 4: Запустите миграцию

1. В выпадающем списке выберите функцию:
   ```
   migrateClients
   ```

2. Нажмите **"Запустить"** (▶️)

3. Скрипт начнёт работу (~1 минута)

4. Вы увидите сообщение:
   ```
   Миграция завершена!

   ✅ Клиентов добавлено: XX
   ⏭️ Клиентов пропущено: XX
   📦 Артикулов добавлено: XX
   ```

5. Нажмите **"OK"**

---

### ШАГ 5: Проверьте результат

Вернитесь в таблицу Rating5_OPS и проверьте:

**Лист `Clients`:**
- [ ] Все клиенты импортированы
- [ ] ClientID автоматически сгенерированы (CLI_001, CLI_002...)
- [ ] Статусы правильные ("Активен" / "В работе" / "Потеря")
- [ ] DriveFolderID заполнены (если были ссылки)
- [ ] ScreenshotsFolderID заполнены
- [ ] Даты CreatedAt и UpdatedAt установлены

**Лист `Articles`:**
- [ ] Артикулы импортированы
- [ ] ArticleID автоматически сгенерированы (ART_00001, ART_00002...)
- [ ] Артикулы привязаны к правильным ClientID
- [ ] Статус "Активен"

---

## 🔍 ЧТО ДЕЛАЕТ СКРИПТ

### Для каждого клиента:

1. **Читает данные** из листа "Клиенты" старой таблицы
2. **Извлекает информацию:**
   - Client Name
   - Статус ("В работе" → "Активен")
   - Папка клиента (URL → извлекает Folder ID)
   - Скриншоты (URL → извлекает Folder ID)
   - Отчет клиента (URL → извлекает Spreadsheet ID)
3. **Генерирует ClientID** (CLI_001, CLI_002...)
4. **Проверяет дубликаты** (если клиент уже есть - пропускает)
5. **Записывает в лист Clients**

### Для артикулов:

1. **Ищет лист "Артикулы"** в старой таблице
2. **Находит колонку** с названием клиента
3. **Читает все артикулы** из этой колонки
4. **Генерирует ArticleID** для каждого
5. **Записывает в лист Articles** с привязкой к ClientID

---

## 📊 СТРУКТУРА СТАРОЙ ТАБЛИЦЫ

Скрипт ожидает такую структуру в старой таблице:

**Лист "Клиенты":**
```
| Client Name | Статус | ... | Папка клиента | Отчет клиента | Скриншоты |
|-------------|--------|-----|---------------|---------------|-----------|
| ИП Иванов   | Активен| ... | https://...   | https://...   | https://...|
```

**Лист "Артикулы":**
```
| ИП Иванов | ИП Петров | ООО Сидоров |
|-----------|-----------|-------------|
| 123456789 | 987654321 | 111222333   |
| 111111111 | 222222222 | 444555666   |
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### Дубликаты

Скрипт **автоматически пропускает** клиентов, которые уже есть в новой таблице (проверка по Client Name).

Если нужно повторно импортировать данные:
1. Удалите строки из листа Clients (кроме заголовка)
2. Удалите строки из листа Articles (кроме заголовка)
3. Запустите миграцию заново

---

### Артикулы не импортировались?

Возможные причины:
- В старой таблице нет листа "Артикулы"
- Название клиента в заголовке не совпадает с Client Name
- Колонка пустая

**Решение:** Добавьте артикулы вручную в лист Articles или исправьте старую таблицу и запустите миграцию заново.

---

### Folder IDs не извлеклись?

Если в колонках DriveFolderID или ScreenshotsFolderID пусто:
- Проверьте, что в старой таблице были ссылки (не просто ID)
- Скрипт поддерживает формат: `https://drive.google.com/drive/folders/FOLDER_ID`
- Если у вас другой формат - измените функцию `extractFolderId()`

---

## 🛠️ TROUBLESHOOTING

### Ошибка: "You do not have permission..."

**Решение:**
- У вас нет доступа к старой таблице
- Откройте старую таблицу в браузере - убедитесь что она открывается
- Проверьте что вы залогинены под тем же Google аккаунтом

---

### Ошибка: "Лист Клиенты не найден"

**Решение:**
- В старой таблице лист называется не "Клиенты"
- Переименуйте лист в "Клиенты" ИЛИ
- Измените название в коде: `getSheetByName('ВашеНазвание')`

---

### Скрипт выполнился, но данные не появились

**Решение:**
- Проверьте логи выполнения: View → Logs
- Возможно все клиенты были пропущены как дубликаты
- Проверьте что в старой таблице есть данные

---

## 📧 АЛЬТЕРНАТИВНЫЙ МЕТОД: Ручной импорт

Если автоматическая миграция не работает:

1. Откройте старую таблицу
2. Экспортируйте лист "Клиенты": Файл → Скачать → CSV (текущий лист)
3. Откройте новую таблицу Rating5_OPS
4. Откройте лист Clients
5. Файл → Импорт → Загрузить → Выберите CSV
6. Выберите "Добавить строки в текущий лист"
7. Вручную извлеките Folder IDs из URLs (или используйте формулы)

---

## ✅ ПРОВЕРКА ПОСЛЕ МИГРАЦИИ

После успешной миграции у вас должно быть:

- [x] Все активные клиенты в листе Clients
- [x] ClientID сгенерированы автоматически
- [x] DriveFolderID заполнены (если были ссылки)
- [x] Все артикулы в листе Articles
- [x] Артикулы привязаны к ClientID

**Следующий шаг:** Написание Apps Script модулей для автоматической отчётности!

Дайте мне знать когда миграция завершится, и я продолжу с Phase 2! 🚀
